EHR_TOKEN_URL=https://fake.example/oauth/token
EHR_CLIENT_ID=dev-client
SERVICE_PRINCIPAL=triage-assistant@dev-vpc

# Admin endpoints bearer token — DEV PLACEHOLDER, not a real secret.
# Production: set via secret manager, see prod.env.example.
ADMIN_BEARER_TOKEN=dev-token-not-secret
