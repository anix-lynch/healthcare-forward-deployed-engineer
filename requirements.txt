# Compatible-release pinning (~=) — locks the major+minor but allows
# patch updates, so CVE patches flow in but breaking API changes don't
# surprise CI. Tighten to == when you cut a customer release.
fastapi~=0.115
uvicorn[standard]~=0.27
pydantic~=2.0
numpy~=1.26
starlette~=0.40

# Agentic / LangGraph (stateful agent with checkpointing)
langgraph~=0.2
langchain-core~=0.2

# Observability — OpenTelemetry partial instrumentation
opentelemetry-api~=1.20
opentelemetry-sdk~=1.20

# Tests
pytest~=7.4
httpx~=0.27
